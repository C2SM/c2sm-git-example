# Example User

- **GitHub:** [@example-user](https://github.com/example-user)
- **Group or institution:** C2SM, ETH Zurich
- **Working on:** an example of what these pages should look like

I learned that `git log --oneline --graph --decorate --all` shows the whole branch structure at
once, and that `git rm --cached` stops tracking a file without deleting it.
